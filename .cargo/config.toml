# No hardcoded linker path — let Rust auto-detect MSVC from the registry.
# On local MSYS2/Git Bash where /usr/bin/link shadows link.exe, run cargo
# from a VS Developer Command Prompt or set:
#   set CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_LINKER=link.exe
