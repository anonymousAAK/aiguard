name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # ---------------------------------------------------------------------------
  # Test gate — must pass before any binaries are built
  # ---------------------------------------------------------------------------
  test:
    name: cargo test (workspace)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache cargo registry + build artifacts
        uses: Swatinem/rust-cache@v2

      - name: Run workspace tests
        run: cargo test --workspace --locked

  # ---------------------------------------------------------------------------
  # Build matrix
  # ---------------------------------------------------------------------------
  build:
    name: build ${{ matrix.target }}
    needs: test
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS — native runners
          - target: aarch64-apple-darwin
            runner: macos-14          # M-series, arm64
            cross: false

          - target: x86_64-apple-darwin
            runner: macos-13          # Intel
            cross: false

          # Linux — use cross for hermetic musl builds
          - target: aarch64-unknown-linux-musl
            runner: ubuntu-latest
            cross: true

          - target: x86_64-unknown-linux-musl
            runner: ubuntu-latest
            cross: true

          # Linux GNU (glibc)
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
            cross: false              # native runner is already x86_64-linux-gnu

          # Windows (pinned to 2022 runner for stable MSVC toolchain)
          - target: x86_64-pc-windows-msvc
            runner: windows-2022
            cross: false

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Cache cargo registry + build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross (if needed)
        if: matrix.cross == true
        run: cargo install cross --git https://github.com/cross-rs/cross --locked

      - name: Setup MSVC dev environment (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build release binary (cross)
        if: matrix.cross == true
        run: cross build --release --locked --target ${{ matrix.target }} -p aiguard

      - name: Build release binary (cargo)
        if: matrix.cross == false
        run: cargo build --release --locked --target ${{ matrix.target }} -p aiguard

      # ---------------------------------------------------------------------------
      # Package the binary
      # ---------------------------------------------------------------------------
      - name: Create release archive (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          TAG="${GITHUB_REF_NAME}"
          TARGET="${{ matrix.target }}"
          ARCHIVE="aiguard-${TAG}-${TARGET}.tar.gz"
          BIN="target/${TARGET}/release/aiguard"

          tar -czf "$ARCHIVE" -C "$(dirname "$BIN")" "$(basename "$BIN")"

          # sha256sum on Linux, shasum on macOS
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$ARCHIVE" > "${ARCHIVE}.sha256"
          else
            shasum -a 256 "$ARCHIVE" > "${ARCHIVE}.sha256"
          fi

          echo "ARCHIVE=${ARCHIVE}" >> "$GITHUB_ENV"
          echo "CHECKSUM=${ARCHIVE}.sha256" >> "$GITHUB_ENV"

      - name: Create release archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $tag    = "$env:GITHUB_REF_NAME"
          $target = "${{ matrix.target }}"
          $archive = "aiguard-$tag-$target.zip"
          $bin    = "target\$target\release\aiguard.exe"

          Compress-Archive -Path $bin -DestinationPath $archive

          $hash = (Get-FileHash -Path $archive -Algorithm SHA256).Hash.ToLower()
          "$hash  $archive" | Out-File -FilePath "$archive.sha256" -Encoding ascii

          Add-Content $env:GITHUB_ENV "ARCHIVE=$archive"
          Add-Content $env:GITHUB_ENV "CHECKSUM=$archive.sha256"

      # ---------------------------------------------------------------------------
      # Upload artifacts for the publish job
      # ---------------------------------------------------------------------------
      - name: Upload archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.target }}
          path: |
            ${{ env.ARCHIVE }}
            ${{ env.CHECKSUM }}
          if-no-files-found: error
          retention-days: 1

  # ---------------------------------------------------------------------------
  # Publish — creates the GitHub Release and uploads all assets
  # ---------------------------------------------------------------------------
  publish:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: dist/
          merge-multiple: true

      - name: List release assets
        run: ls -lh dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "aiguard ${{ github.ref_name }}"
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: true
          files: dist/*
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
